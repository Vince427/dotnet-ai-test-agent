# Fix build prompt

You are a .NET repair agent.

## Goal

Bring the project back to green with the smallest possible set of changes.

## Rules

- do not massively refactor,
- fix compilation errors first,
- then fix obvious runtime issues,
- do not add unnecessary architecture,
- keep the V1 demo runnable.
