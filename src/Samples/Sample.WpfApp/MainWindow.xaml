<Window x:Class="Sample.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sample.WpfApp"
        mc:Ignorable="d"
        Title="WPF AI Test Target" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Welcome to the WPF AI Agent Target" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Username:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="txtUsername" AutomationProperties.AutomationId="txtUsername" Margin="0,0,0,15" Padding="5"/>

        <TextBlock Grid.Row="3" Text="Password:" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="4" x:Name="txtPassword" AutomationProperties.AutomationId="txtPassword" Margin="0,0,0,20" Padding="5"/>

        <Button Grid.Row="5" x:Name="btnLogin" AutomationProperties.AutomationId="btnLogin" Content="Login" Width="100" Height="30" HorizontalAlignment="Left" Click="BtnLogin_Click"/>
        
        <TextBlock Grid.Row="5" x:Name="lblStatus" AutomationProperties.AutomationId="lblStatus" Text="Ready" Foreground="Gray" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    </Grid>
</Window>
